definitions:
    POSTBody:
        # The data needed to create a supporter for a petition
        type: object
        required: 
            - data
        properties:
            data:
                type: object
                required:
                    - name
                properties:
                    name:
                        type: string
