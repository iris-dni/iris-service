petitions_contentId_state_transitionName:
  options:
    tags:
      - petition
    summary: CORS Options
    parameters:
      - name: contentId
        in: path
        type: integer
      - name: transitionName
        in: path
        type: string
    responses:
      default:
        description: Empty Body
  post:
    tags:
      - petition
    summary: Generic state manipulation
    operationId: state
    parameters:
      - name: contentId
        in: path
        type: integer
      - name: transitionName
        in: path
        type: string
    responses:
      default:
        description: Unexpected error
        schema:
          $ref: 'errors.yml#/definitions/ResponseBody'
petitions_contentId_state_reject:
  options:
    tags:
      - petition
    summary: CORS Options
    parameters:
      - name: contentId
        in: path
        type: integer
      - name: transitionName
        in: path
        type: string
    responses:
      default:
        description: Empty Body
  post:
    tags:
      - petition
    summary: Reject a Petition
    operationId: reject
    parameters:
      - name: contentId
        in: path
        type: integer
      - name: transitionName
        in: path
        type: string
      - name: data
        in: body
        schema:
          $ref: 'state.yml#/definitions/rejectData'
        required: true
    responses:
      default:
        description: Unexpected error
        schema:
          $ref: 'errors.yml#/definitions/ResponseBody'
