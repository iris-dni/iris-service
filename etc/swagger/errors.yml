definitions:
    ResponseBody:
        # The error response format for all none 2xx response codes
        type: object
        required: 
            - code
            - description
        properties:
            code:
                type: string
            description:
                type: string
